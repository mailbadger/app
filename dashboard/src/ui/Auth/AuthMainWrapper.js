import styled from 'styled-components';
import { Box } from 'grommet';

const AuthMainWrapper = styled(Box)`
    position: absolute;
    top: 50%;
    transform: translatey(-50%);
`;

export default AuthMainWrapper;
