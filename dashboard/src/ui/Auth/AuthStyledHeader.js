import { Heading } from 'grommet';
import styled from 'styled-components';

const AuthStyledHeader= styled(Heading)`
    margin: 0;
    font-size: 40px;
    color: #000;
    line-height: 60px;
`;

export default AuthStyledHeader;
 